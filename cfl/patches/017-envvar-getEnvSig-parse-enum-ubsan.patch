diff --git a/IccProfLib/IccMpeCalc.cpp b/IccProfLib/IccMpeCalc.cpp
index 1f68f4b..976644a 100644
--- a/IccProfLib/IccMpeCalc.cpp
+++ b/IccProfLib/IccMpeCalc.cpp
@@ -2797,7 +2797,7 @@ icFloat32Number CIccFuncTokenizer::GetValue()
 }
 
 
-bool CIccFuncTokenizer::GetEnvSig(icSigCmmEnvVar &envSig)
+bool CIccFuncTokenizer::GetEnvSig(icUInt32Number &envSig)  // CFL-017: icUInt32Number avoids enum UBSAN (CWE-681)
 {
   const char *pos = GetPos();
 
@@ -2814,7 +2814,7 @@ bool CIccFuncTokenizer::GetEnvSig(icSigCmmEnvVar &envSig)
     if (l==10) {
       sscanf(szToken+1, "%x", &sig);
 
-      envSig = (icSigCmmEnvVar)sig;
+      envSig = sig;
       return true;
     }
 
@@ -2832,7 +2832,7 @@ bool CIccFuncTokenizer::GetEnvSig(icSigCmmEnvVar &envSig)
   }
   else {
     SetPos(pos); //Undo get token
-    envSig = (icSigCmmEnvVar)0;
+    envSig = 0;
     return false;
   }
 
@@ -3251,8 +3251,8 @@ const char *CIccCalculatorFunc::ParseFuncDef(const char *szFuncDef, CIccCalcOpLi
 
       case icSigEnvVarOp:
         {
-          icSigCmmEnvVar envSig;
-          if (!scan.GetEnvSig(envSig))
+          icUInt32Number envSig;  // CFL-017: parse as raw uint32 to avoid enum UBSAN
+          if (!scan.GetEnvSig(envSig))  
             return NULL;
           op.data.size = (icUInt32Number)envSig;
           scanList.push_back(op);
diff --git a/IccProfLib/IccMpeCalc.h b/IccProfLib/IccMpeCalc.h
index 3d93ac5..3bf95d3 100644
--- a/IccProfLib/IccMpeCalc.h
+++ b/IccProfLib/IccMpeCalc.h
@@ -334,7 +334,7 @@ public:
   icSigCalcOp GetSig();
   bool GetIndex(icUInt16Number &v1, icUInt16Number &v2, icUInt16Number initV1=0, icUInt16Number initV2=0);
   icFloat32Number GetValue();
-  bool GetEnvSig(icSigCmmEnvVar &envSig);
+  bool GetEnvSig(icUInt32Number &envSig);  // CFL-017: avoid enum UBSAN on untrusted XML input (CWE-681)
 
   //The following are used by XML variable and macro parsing
   std::string GetName() const;
