diff --git a/IccProfLib/IccMpeCalc.cpp b/IccProfLib/IccMpeCalc.cpp
index b1a2c3d..c2d3e4f 100644
--- a/IccProfLib/IccMpeCalc.cpp
+++ b/IccProfLib/IccMpeCalc.cpp
@@ -3749,8 +3749,14 @@ bool CIccCalculatorFunc::ApplySequence(CIccApplyMpeCalculator *pApply, icUInt32N
       icInt32Number nSel;
       if (std::isinf(a1)) {
         nSel = (a1 < 0.0) ? std::numeric_limits<icInt32Number>::lowest() : std::numeric_limits<icInt32Number>::max();
       }
-      else
-        nSel = (a1 >= 0.0) ? (icInt32Number)(a1+0.5f) : (icInt32Number)(a1-0.5f);
+      else {
+        icFloatNumber rounded = (a1 >= 0.0) ? (a1+0.5f) : (a1-0.5f);
+        if (rounded >= (icFloatNumber)std::numeric_limits<icInt32Number>::max())
+          nSel = std::numeric_limits<icInt32Number>::max();
+        else if (rounded <= (icFloatNumber)std::numeric_limits<icInt32Number>::lowest())
+          nSel = std::numeric_limits<icInt32Number>::lowest();
+        else
+          nSel = (icInt32Number)rounded;
+      }
 
       if (!op->extra) {
         return false;
