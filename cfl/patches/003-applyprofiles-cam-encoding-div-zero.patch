diff --git a/IccProfLib/IccCAM.cpp b/IccProfLib/IccCAM.cpp
--- a/IccProfLib/IccCAM.cpp
+++ b/IccProfLib/IccCAM.cpp
@@ -150 +150,7 @@ CIccCamConverter::H_FunctionInv (icFloatNumber	y)
-	x = (icFloatNumber) (pow (27.13*y / (400.0-y), 1.0/m_exp));
+	double denom = 400.0 - (double)y;
+	if (!std::isfinite((double)y) || !std::isfinite((double)m_exp) || m_exp == 0.0f ||
+	    !std::isfinite(denom) || denom == 0.0) {
+		return 0.0f;
+	}
+
+	x = (icFloatNumber) (pow (27.13*y / denom, 1.0/m_exp));
diff --git a/IccProfLib/IccEncoding.cpp b/IccProfLib/IccEncoding.cpp
--- a/IccProfLib/IccEncoding.cpp
+++ b/IccProfLib/IccEncoding.cpp
@@ -435 +435,3 @@ icStatusEncConvert CIccDefaultEncProfileConverter::ConvertFromParams(CIccProfile
-    icFloatNumber SWr = Lsw / Lw;
+    icFloatNumber SWr = 0.0f;
+    if (std::isfinite(Lsw) && std::isfinite(Lw) && Lw != 0.0f)
+      SWr = Lsw / Lw;
