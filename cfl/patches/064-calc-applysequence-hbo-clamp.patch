--- a/IccProfLib/IccMpeCalc.cpp	2026-03-05 20:11:54.142153072 -0500
+++ b/IccProfLib/IccMpeCalc.cpp	2026-03-05 20:12:14.930061599 -0500
@@ -3717,31 +3717,38 @@
 
       if (os.idx+1<nOps && ops[os.idx+1].sig==icSigElseOp) {
         os.idx++;
+        icUInt32Number remaining = nOps - (os.idx + 1);
+        icUInt32Number ifSize = (op->data.size <= remaining) ? op->data.size : remaining;
+        icUInt32Number elseSize = (ops[os.idx].data.size <= (remaining - ifSize)) ? ops[os.idx].data.size : (remaining - ifSize);
+
         if (a1>=0.5) {
-          if (os.idx+1 + op->data.size >= nOps)
+          if (os.idx+1 + ifSize > nOps)
             return false;
 
-          if (!ApplySequence(pApply, op->data.size, &ops[os.idx+1]))
+          if (ifSize && !ApplySequence(pApply, ifSize, &ops[os.idx+1]))
             return false;
         }
         else {
-          if (os.idx+1 + op->data.size + ops[os.idx].data.size > nOps)
+          if (os.idx+1 + ifSize + elseSize > nOps)
             return false;
 
-          if (!ApplySequence(pApply, ops[os.idx].data.size, &ops[os.idx+1 + op->data.size]))
+          if (elseSize && !ApplySequence(pApply, elseSize, &ops[os.idx+1 + ifSize]))
             return false;
         }
-        os.idx += op->data.size + ops[os.idx].data.size;
+        os.idx += ifSize + elseSize;
       }
       else {
+        icUInt32Number remaining = nOps - (os.idx + 1);
+        icUInt32Number ifSize = (op->data.size <= remaining) ? op->data.size : remaining;
+
         if (a1>=0.5) {
-          if (os.idx+op->data.size >= nOps)
+          if (os.idx + ifSize >= nOps)
             return false;
 
-          if (!ApplySequence(pApply, op->data.size, &ops[os.idx+1]))
+          if (ifSize && !ApplySequence(pApply, ifSize, &ops[os.idx+1]))
             return false;
         }
-        os.idx+= op->data.size;
+        os.idx += ifSize;
       }
     }
     else if (op->sig==icSigSelectOp) {
