diff --git a/IccProfLib/IccTagLut.cpp b/IccProfLib/IccTagLut.cpp
index b60b7a4..0cc06b1 100644
--- a/IccProfLib/IccTagLut.cpp
+++ b/IccProfLib/IccTagLut.cpp
@@ -2492,6 +2492,7 @@ void CIccCLUT::Interp1d(icFloatNumber *destPixel, const icFloatNumber *srcPixel)
   icUInt8Number mx = m_MaxGridPoint[0];
 
   icFloatNumber x = UnitClip(srcPixel[0]) * mx;
+  if (x < 0.0f) x = 0.0f;
 
   icUInt32Number ix = (icUInt32Number)x;
 
@@ -2605,6 +2606,9 @@ void CIccCLUT::Interp3dTetra(icFloatNumber *destPixel, const icFloatNumber *srcP
   icFloatNumber x = UnitClip(srcPixel[0]) * mx;
   icFloatNumber y = UnitClip(srcPixel[1]) * my;
   icFloatNumber z = UnitClip(srcPixel[2]) * mz;
+  if (x < 0.0f) x = 0.0f;
+  if (y < 0.0f) y = 0.0f;
+  if (z < 0.0f) z = 0.0f;
 
   icUInt32Number ix = (icUInt32Number)x;
   icUInt32Number iy = (icUInt32Number)y;
@@ -2691,6 +2695,9 @@ void CIccCLUT::Interp3d(icFloatNumber *destPixel, const icFloatNumber *srcPixel)
   icFloatNumber x = UnitClip(srcPixel[0]) * mx;
   icFloatNumber y = UnitClip(srcPixel[1]) * my;
   icFloatNumber z = UnitClip(srcPixel[2]) * mz;
+  if (x < 0.0f) x = 0.0f;
+  if (y < 0.0f) y = 0.0f;
+  if (z < 0.0f) z = 0.0f;
 
   icUInt32Number ix = (icUInt32Number)x;
   icUInt32Number iy = (icUInt32Number)y;
@@ -2763,6 +2770,10 @@ void CIccCLUT::Interp4d(icFloatNumber *destPixel, const icFloatNumber *srcPixel)
   icFloatNumber x = UnitClip(srcPixel[1]) * mx;
   icFloatNumber y = UnitClip(srcPixel[2]) * my;
   icFloatNumber z = UnitClip(srcPixel[3]) * mz;
+  if (w < 0.0f) w = 0.0f;
+  if (x < 0.0f) x = 0.0f;
+  if (y < 0.0f) y = 0.0f;
+  if (z < 0.0f) z = 0.0f;
 
   icUInt32Number iw = (icUInt32Number)w;
   icUInt32Number ix = (icUInt32Number)x;
@@ -2851,6 +2862,11 @@ void CIccCLUT::Interp5d(icFloatNumber *destPixel, const icFloatNumber *srcPixel)
   icFloatNumber g2 = UnitClip(srcPixel[2]) * m2;
   icFloatNumber g3 = UnitClip(srcPixel[3]) * m3;
   icFloatNumber g4 = UnitClip(srcPixel[4]) * m4;
+  if (g0 < 0.0f) g0 = 0.0f;
+  if (g1 < 0.0f) g1 = 0.0f;
+  if (g2 < 0.0f) g2 = 0.0f;
+  if (g3 < 0.0f) g3 = 0.0f;
+  if (g4 < 0.0f) g4 = 0.0f;
 
   icUInt32Number ig0 = (icUInt32Number)g0;
   icUInt32Number ig1 = (icUInt32Number)g1;
@@ -2965,6 +2981,12 @@ void CIccCLUT::Interp6d(icFloatNumber *destPixel, const icFloatNumber *srcPixel)
   icFloatNumber g3 = UnitClip(srcPixel[3]) * m3;
   icFloatNumber g4 = UnitClip(srcPixel[4]) * m4;
   icFloatNumber g5 = UnitClip(srcPixel[5]) * m5;
+  if (g0 < 0.0f) g0 = 0.0f;
+  if (g1 < 0.0f) g1 = 0.0f;
+  if (g2 < 0.0f) g2 = 0.0f;
+  if (g3 < 0.0f) g3 = 0.0f;
+  if (g4 < 0.0f) g4 = 0.0f;
+  if (g5 < 0.0f) g5 = 0.0f;
 
   icUInt32Number ig0 = (icUInt32Number)g0;
   icUInt32Number ig1 = (icUInt32Number)g1;
@@ -3112,6 +3134,7 @@ void CIccCLUT::InterpND(icFloatNumber *destPixel, const icFloatNumber *srcPixel,
 
   for (i=0; i<m_nInput; i++) {
     g[i] = UnitClip(srcPixel[i]) * m_MaxGridPoint[i];
+    if (g[i] < 0.0f) g[i] = 0.0f;
     ig[i] = (icUInt32Number)g[i];
     s[m_nInput-1-i] = g[i] - ig[i];
     if (ig[i]==m_MaxGridPoint[i]) {
