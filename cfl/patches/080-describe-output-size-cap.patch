--- a/IccProfLib/IccTagBasic.cpp
+++ b/IccProfLib/IccTagBasic.cpp
@@ -3755,6 +3755,10 @@
 void CIccTagXYZ::Describe(std::string &sDescription, int /* nVerboseness */)
 {
   const size_t bufSize = 128;
+  // CFL-080: Cap Describe() output to 1MB (CWE-400)
+  static const size_t kMaxDescribeSize = 1048576;
+  if (sDescription.size() > kMaxDescribeSize)
+    return;
   icChar buf[bufSize];
 
   if (m_nSize == 1 ) {
@@ -3767,6 +3771,7 @@
 
     for (i=0; i<m_nSize; i++) {
       snprintf(buf, bufSize, "value[%u]: X=%.4lf, Y=%.4lf, Z=%.4lf\n", i, icFtoD(m_XYZ[i].X), icFtoD(m_XYZ[i].Y), icFtoD(m_XYZ[i].Z));
+      if (sDescription.size() > kMaxDescribeSize) break;
       sDescription += buf;
     }
   }
