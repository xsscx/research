--- a/IccProfLib/IccTagLut.cpp
+++ b/IccProfLib/IccTagLut.cpp
@@ -520,14 +520,15 @@ bool CIccTagCurve::SetGamma(icFloatNumber gamma)
 *****************************************************************************
 */
 bool CIccTagCurve::SetGamma(icFloatNumber gamma)
 {
+  const icFloatNumber maxGamma = (icFloatNumber)(65535.0f / 256.0f);
+  if (!std::isfinite(static_cast<double>(gamma)) || gamma < 0.0f || gamma > maxGamma)
+    return false;
+
   if (!SetSize(1, icInitNone))
     return false;

-  icInt16Number whole = (icInt16Number)gamma;
-  icFloatNumber frac = gamma - (icFloatNumber)whole;
-
-  m_Curve[0] = (icFloatNumber)((whole * 256) + (frac*256.0)) / (icFloatNumber)65535.0;
+  m_Curve[0] = (gamma * 256.0f) / (icFloatNumber)65535.0f;
   return true;
 }

--- a/IccJSON/IccLibJSON/IccTagJson.cpp
+++ b/IccJSON/IccLibJSON/IccTagJson.cpp
@@ -1704,8 +1704,9 @@ bool CIccTagJsonCurve::ParseJson(const IccJson &j, icConvertType /*nType*/, std
   } else if (curveType == "gamma") {
     SetSize(1);
     double gamma = 1.0;
     jGetValue(j, "gamma", gamma);
-    SetGamma((icFloatNumber)gamma);
+    if (!SetGamma((icFloatNumber)gamma))
+      return false;
   } else {
     if (!jsonExistsField(j, "table") || !j["table"].is_array()) return false;
     const IccJson &arr = j["table"];
