name: 'Setup Build Environment'
description: 'Install build dependencies for ICC security research tools'
inputs:
  preset:
    description: 'Package preset: analyzer, cfl, colorbleed, codeql, coverage, minimal'
    required: false
    default: 'analyzer'
  extra-packages:
    description: 'Additional apt packages (space-separated)'
    required: false
    default: ''

runs:
  using: 'composite'
  steps:
    - name: Install build dependencies
      shell: bash
      env:
        BASH_ENV: /dev/null
        DEBIAN_FRONTEND: noninteractive
        PRESET: ${{ inputs.preset }}
        EXTRA_PACKAGES: ${{ inputs.extra-packages }}
      run: |
        set -euo pipefail
        git config --global credential.helper ""
        unset GITHUB_TOKEN || true

        COMMON="build-essential cmake libxml2-dev libtiff-dev libpng-dev zlib1g-dev nlohmann-json3-dev"

        case "${PRESET}" in
          analyzer)
            PACKAGES="${COMMON} clang libjpeg-dev libssl-dev liblzma-dev"
            ;;
          cfl)
            PACKAGES="${COMMON} clang clang-tools libssl-dev llvm"
            ;;
          colorbleed)
            PACKAGES="${COMMON} gcc g++ clang clang-tools libjpeg-dev libssl-dev llvm python3 python3-pip curl git"
            ;;
          codeql)
            PACKAGES="${COMMON} clang libjpeg-dev libssl-dev git ca-certificates"
            ;;
          coverage)
            PACKAGES="${COMMON} clang-18 llvm-18 libjpeg-dev lcov"
            ;;
          minimal)
            PACKAGES="${COMMON} clang"
            ;;
          *)
            echo "[FAIL] Unknown preset: ${PRESET}"
            exit 1
            ;;
        esac

        if [ -n "${EXTRA_PACKAGES}" ]; then
          PACKAGES="${PACKAGES} ${EXTRA_PACKAGES}"
        fi

        sudo apt-get update -qq
        sudo apt-get install -y --no-install-recommends ${PACKAGES}
        echo "[OK] Installed packages for preset: ${PRESET}"
