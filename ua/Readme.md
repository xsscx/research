# UA -- User Agent Image & ICC Profile Fuzzer Harness

Browser-based security testing harness that exercises user agent image decoders
and ICC color profile parsers via four attack modes. Runs entirely client-side --
no server logic, no dependencies beyond a static HTTP server.

<img width="3672" height="1917" alt="image" src="https://github.com/user-attachments/assets/8092db67-8705-4cef-8aef-f2a25afaa421" />

## Quick Start

```bash
# From the repository root
cd ua
python3 generate-manifest.py --root ..
cd ..
python3 -m http.server 8080
# Open http://localhost:8080/ua/ua-image-icc-fuzzer.html
```

## Four Attack Modes

### 1. Image/ICC (default)

Original V2 mode. Batch-loads images and ICC profiles from the manifest.
Images go through `<img>` load -> canvas decode (Display P3 color space) ->
pixel readback -> quality checks. ICC profiles go through `fetch()` ->
ArrayBuffer -> Web Worker pool for off-main-thread binary header parsing.

### 2. CSS @color-profile

CSS Level 4 `@color-profile` testing. Each ICC profile is loaded via
`@color-profile --name { src: url(file.icc); }` and applied using
`color(--name 1 0 0)`. This exercises a completely different browser code
path than Image/ICC mode -- specifically `CGColorSpaceCreateWithICCData` /
ColorSync on WebKit/macOS. Derived from `~/webkit/icctest.html` technique.

### 3. Memory Stress

Spawns N Web Workers each allocating M megabytes of ArrayBuffer memory
while simultaneously processing ICC profiles on the main thread. Memory
corruption bugs often manifest only under allocation pressure. Based on
the `webkit-targeted-worker-memory-fragmentation-crash.html` PoC which
produced a confirmed arm64 PAC_EXCEPTION crash in Safari 17.6.

Workers perform: initial allocation (10 rounds), heap fragmentation
(splice holes + reallocate), and concurrent ICC parsing on the main thread.

### 4. CSS Filter/Transform

Loads ICC-bearing images into 64x64 canvases with randomized CSS filter
and transform combinations. Each image gets N variants with random
`blur()`, `contrast()`, `grayscale()`, `invert()`, `brightness()`,
`saturate()`, `sepia()`, `hue-rotate()`, `drop-shadow()` filter chains
and `scale()`, `rotate()`, `skewX()`, `skewY()`, `perspective()`,
`rotate3d()` transforms. Exercises GPU compositing + color space conversion
intersection. Derived from `~/webkit/location-href-testing-001.html`
technique (100 CSS filter PoCs).

## Architecture

### Layout

| Panel | Content |
|-------|---------|
| **Mode Tabs** (header) | Switch between 4 attack modes |
| **Render** (left) | Live image display + ICC info overlay |
| **Log** (center) | Timestamped event stream (OK/WARN/ERR/CRASH) |
| **Findings** (right) | CWE-annotated security findings with severity |

### Components

| File | Purpose |
|------|---------|
| `ua-image-icc-fuzzer.html` | V3 fuzzer harness (single-file, ~1160 LOC) |
| `manifest.json` | File inventory generated by `generate-manifest.py` |
| `generate-manifest.py` | Scans repo for image/ICC/XML files |

### ICC Worker Analysis (off-main-thread)

Web Workers parse ICC binary headers and perform:
- Header field validation (size, magic, version BCD, rendering intent)
- PCS illuminant D50 check (s15Fixed16 fixed-point comparison)
- Reserved bytes validation (bytes 100-127 must be zero)
- Profile ID extraction (bytes 84-99)
- Tag table walk with bounds checking
- **Duplicate signature detection** (CWE-694)
- **Tag overlap detection** -- partial overlaps flagged (CWE-119)
- **Tag alignment check** -- 4-byte alignment per ICC spec
- Memory stress via ArrayBuffer fragmentation (exercises GC)

### Image Quality Checks

- Flat/degenerate detection (all pixels same color)
- Fully transparent detection (all alpha = 0)
- Extreme dimension detection (>16384 in either axis)
- Zero-dimension detection (loaded without error)
- createImageBitmap alternate decode path exercise

### CWE Classification

All findings are classified with CWE codes and severity levels:

| CWE | Severity | Pattern |
|-----|----------|---------|
| CWE-119 | CRITICAL | Buffer error, tag overlap |
| CWE-122 | CRITICAL | Heap buffer overflow (tag overflows) |
| CWE-131 | HIGH | Size mismatch |
| CWE-345 | CRITICAL | Invalid magic number |
| CWE-400 | HIGH | Resource exhaustion (timeout, extreme dims) |
| CWE-682 | MEDIUM | PCS illuminant deviation |
| CWE-789 | HIGH | Suspicious tag count |
| CWE-843 | CRITICAL | ICC decoded as image (type confusion) |

## Manifest Generation

```bash
# Default scan (test-profiles, fuzz, mangled-images, iccDEV/Testing, source-of-truth)
python3 generate-manifest.py --root ..

# Full scan (includes CFL/AFL corpus directories)
python3 generate-manifest.py --root .. --full

# Stats only (no file write)
python3 generate-manifest.py --root .. --stats
```

The manifest classifies files by extension and ICC magic bytes (`acsp` at offset 36).
Output: `manifest.json` with `{total, stats, files[{path, type, ext}]}`.

## Export Formats

- **JSON**: Full findings with path, detail, severity, CWE, timestamp
- **CSV**: Tabular format for spreadsheet analysis

## Keyboard Shortcuts

| Key | Action |
|-----|--------|
| Space | Start/Stop |
| S | Shuffle files |
| M | Show summary |
| 1 | Image/ICC mode |
| 2 | CSS Profile mode |
| 3 | Memory Stress mode |
| 4 | CSS Filter mode |

## Controls

| Control | Default | Range | Purpose |
|---------|---------|-------|---------|
| Type | All | All/Images/ICC/XML/per-format | File type filter |
| Delay | 20ms | 0-5000ms | Inter-batch delay |
| Batch | 8 | 1-64 | Files per batch |
| Workers | 4 | 0-32 | ICC parser Web Workers |
| Stress Workers | 16 | 1-200 | Memory pressure workers (Stress mode) |
| Chunk MB | 50 | 1-500 | Per-worker allocation (Stress mode) |
| ICC batch | 4 | 1-32 | Concurrent ICC loads under stress |
| Variants | 8 | 1-50 | Filter combos per image (Filter mode) |

## Requirements

- Modern browser (Chrome 90+, Firefox 90+, Safari 15+)
- Local HTTP server (CORS requires same-origin for fetch)
- Repository checked out (manifest paths are relative)

## Relationship to Other Components

| Component | Relationship |
|-----------|-------------|
| `test-profiles/` | Primary ICC profile source (329 profiles) |
| `extended-test-profiles/` | CVE PoC profiles (116 files) |
| `fuzz/graphics/` | Malformed images (832 files) |
| `mangled-images/` | Additional malformed test images |
| `iccDEV/Testing/` | iccDEV reference test profiles |
| `cfl/` | CFL fuzzers (complementary -- native vs browser) |
| `iccanalyzer-lite/` | Native 173-heuristic analyzer (complementary) |
| `~/webkit/` | Source PoC techniques for CSS Profile and Stress modes |

## Security Note

This harness processes **intentionally malicious** files. All processing happens
client-side in the browser sandbox. The Web Worker pool isolates ICC parsing from
the main thread. Findings indicate potential browser decoder vulnerabilities.
